name: Test and Build

on:
  push:
    branches:
    - front
    paths:
    - '**/*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    #Setting up environment
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Docker Setup
      uses: docker/setup-buildx-action@v2

    - name: Docker Credentials
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Docker tag
      id: version
      run: |
        VERSION=v$(date +"%Y%m%d%H%M%S")
        echo "VERSION=$VERSION" >> $GITHUB_ENV

    # Build the Docker Image
    - name: Build Docker Image
      run: |
        docker build -t sibasish934/feature-app-frontend:${{ env.VERSION }} feature-extraction-frontend-main/Dockerfile

    # Push the Docker Image
    - name: Push Docker Image
      run: |
        docker push sibasish934/feature-app-frontend:${{ env.VERSION }}

    # UPdate the K8s Manifest Files
    - name: Update K8s Manifests
      run: |
        cat deploy/deploy.yaml
        sed -i "s|image: sibasish934/feature-app-frontend:.*|image: sibasish934/feature-app-frontend:${{ env.VERSION }}|g" deploy/deploy.yaml
        cat deploy/deploy.yaml

    # Update Github
    - name: Commit the changes
      run: |
        git config --global user.email "<>"
        git config --global user.name "GitHub Actions Bot"
        git checkout front
        git add feature-extraction-frontend-main/deploy/deployment.yaml
        git commit -m "Update deploy.yaml with new image version - ${{ env.VERSION }}"
        git push origin front
